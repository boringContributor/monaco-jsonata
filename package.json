{
  "name": "monaco-jsonata",
  "version": "1.0.0",
  "private": true,
  "description": "Monaco JSONata - Language extension and playground",
  "packageManager": "pnpm@10.17.1",
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build",
    "build:lib": "turbo run build --filter=monaco-jsonata",
    "lint": "turbo run lint"
  },
  "devDependencies": {
    "turbo": "^2.6.1"
  },
  "engines": {
    "node": ">=18",
    "pnpm": ">=8"
  }
}
